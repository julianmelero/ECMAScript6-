// Includes

// Sirve para conocer si hay un elemento deentro de este valor

let numbers = [1,2,3,7,8];

// var.includes(elemento a buscar, posición donde se empieza)

if(numbers.includes(7,0)){
    console.log('Sí se encuentra');
}

// Potencias

let base = 4;
let exp = 3;
let result = base**exp;
console.log(result); //64

